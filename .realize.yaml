settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: backend
  path: .
  commands: 
    run:
      status: true
  watcher:
    extensions:
    - go
    - toml
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
